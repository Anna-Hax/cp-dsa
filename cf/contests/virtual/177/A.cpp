#include <bits/stdc++.h>
#include <vector>
#include <iostream>
using namespace std;
using ll = long long;

int main()
{
    cin.tie(nullptr);
    cout.tie(nullptr);

    ll t;
    cin >> t;
    while (t--){

        ll n;
        cin >> n;

        ll answer = (3*n)/(2*0.75);
        cout << answer << endl;
        }
}
